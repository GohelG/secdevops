# Trivy.ymal
scan:
  type: 'image'
  format: 'json'
  exit:code: '1'
  severity: 'HIGH,CRITICAL'

report:
  output: 'trivy_report.html'

vuln:
  type: 'os, library'
  ignorefile: '.trivyignor'    
